{
  "name": "challange",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start":"concurrently --kill-others \"npm start -w frontend\" \"npm start -w api\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/api",
    "packages/frontend"
  ],
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
